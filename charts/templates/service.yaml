apiVersion: v1
kind: Service
metadata:
  name: {{ include "qcon-api.fullname" . }}
  labels:
    {{- include "qcon-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qcon-api.selectorLabels" . | nindent 4 }}
